apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nitro-shared
  namespace: nitro
  labels:
    app: nitro-shared
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: local-path
